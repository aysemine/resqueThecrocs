[project]
name = "resquethecrocs"
version = "0.1.0"
description = "This is a crocodile prediction website"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.11.0",
]
